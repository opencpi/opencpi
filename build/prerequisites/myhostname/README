This library spoofs the build host name for the current platform OCPI_TOOL_PLATFORM
This prereq builds a library for LD_PRELOADing

To use:
1. Include the myhostname.mk file in the Makefile
2. Call rpmbuild or any other program with $(SPOOF_HOSTNAME) prefix

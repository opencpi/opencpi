This directory holds platform build files that are platform independent.

The mkOCApp_bb.v is a sort of univeral container interface that includes
the union of all device capabilities.

importBsv copies verilog files from an HDL imports tree.

the rest of the *.v files are those common infrastructure files.

bitgen_bit.ut is the common settings for bitstream file generation.



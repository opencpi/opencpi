This repo holds the patch file and spec file for building "lzma" (really xz) on Centos 6/7.

It only installs the object files and the header files if you run "./build.sh".

It will build RPMs for the host system and target platform(s) if you run "./build.sh rpm".
